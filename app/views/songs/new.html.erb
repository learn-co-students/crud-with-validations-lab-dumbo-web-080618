<% if @song.errors.any? %>
<ul>
  <% @song.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
  <% end %>
</ul>
<% end %>

<h1>New Song</h1>
<%= render partial: "form", locals: {song: @song} %>
